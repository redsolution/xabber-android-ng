<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="40dp"
    android:layout_marginStart="2dp"
    android:layout_marginTop="2dp"
    android:layout_marginEnd="2dp"
    android:layout_marginBottom="2dp">

    <androidx.camera.view.PreviewView
        android:id="@+id/preview_camera"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@drawable/round_corners_10"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_camera_preview"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="Click to allow camera access"
        android:gravity="center"
        android:textColor="@color/white"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/preview_camera"
        app:layout_constraintEnd_toEndOf="@id/preview_camera"
        app:layout_constraintStart_toStartOf="@id/preview_camera"
        app:layout_constraintTop_toTopOf="@id/preview_camera" />

    <ImageView
        android:id="@+id/image_view_preview_camera"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/ic_camera"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@id/preview_camera"
        app:layout_constraintEnd_toEndOf="@id/preview_camera"
        app:layout_constraintStart_toStartOf="@id/preview_camera"
        app:layout_constraintTop_toTopOf="@id/preview_camera" />


</androidx.constraintlayout.widget.ConstraintLayout>